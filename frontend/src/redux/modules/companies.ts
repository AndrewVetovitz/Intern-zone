const LOAD = 'intern-zone/companies/LOAD';
const LOAD_SUCCESSFUL = 'intern-zone/companies/LOAD_SUCCESFUL';
const LOAD_FAILURE = 'intern-zone/companies/LOAD_FAILURE';

const initialState = {
    loaded: false
}

export default function reducer(state: object = {}, action: object = {}) {

}
